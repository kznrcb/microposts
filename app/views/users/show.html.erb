<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= gravatar_for @user %>
        <%= @user.name %>
      </h1>
    </section>
    
    <%= render 'shared/stats' %>

    <div class="row">
        <%= form_for(@user) do |f| %>
          
          <%= f.label :name %>
          <p><%=@user.name %></p>
    
          <%= f.label :email %>
          <p><%=@user.email %></p>
    
          <%= f.label :password %>
          <p>**********</p>
    
          <%= f.label :profile %>
          <p><%= registrated_data_for(@user.profile)%></p>
          
          <%= f.label :birthday %>
          <p><%= registrated_data_for(@user.birthday)%></p>
    
          <%= f.label :gender %>
           <p><%= registrated_data_for(@user.gender)%></p>
    
          <%= f.label :area %>
          <p><%= registrated_data_for(@user.area)%></p>
    
        <% end %>
    </div>
  
  </aside>
  
  <div class="col-md-8">
  <!--<div class="panel panel-default">-->
  
  <!--
    <div class="panel-heading">
      <ul class="nav nav-pills">
  	    <li role="presentation">
  	      <a href="#">Microposts <span class="badge">99</span></a>
  	    </li>
  	    <li role="presentation">
  	      <a href="#">Followings <span class="badge">99</span></a>
  	    </li>
  	    <li role="presentation">
  	      <a href="#">Followers <span class="badge">99</span></a>
  	    </li>
      </ul>
    </div>
  -->
  
    <!-- <div class="panel-body"> -->
      <%= render 'follow_form',user: @user if logged_in? %>
      <% if @user.microposts.any? %>
        <h3>Microposts (<%= @user.microposts.count %>)</h3>
        <ol class="microposts">
          <%= render @microposts %>
        </ol>
      <% end %>
    <!-- </div> -->
  <!--</div>-->
  </div>
</div>

